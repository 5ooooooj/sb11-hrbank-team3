spring:
  application:
    name: hrbank3
  profiles:
    active: dev
  mvc:
    format:
      date-time: iso # 프론트 파싱을 위한 설정 -> 테스트 필요

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
