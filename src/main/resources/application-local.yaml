spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    username: sa
    password:

  sql:
    init:
      mode: always
      schema-locations: classpath:schema-h2.sql

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: ab779163e32889
    password: eab12b55131a0b
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

file:
  upload:
    path: ./uploads/

backup:
  schedule:
    cron: "-"

notification:
  admin-email: admin@hrbank.com